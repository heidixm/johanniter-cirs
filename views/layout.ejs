<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title><%= title || "CIRS Johanniter Österreich" %></title>
  <link rel="icon" href="/logo.png">
  <style>
    :root { --red:#b0001c; --bg:#f7f7f7; --text:#222; --muted:#666; --ok:#0a7a2a; }
    body { margin:0; font-family:system-ui,Arial,sans-serif; background:var(--bg); color:var(--text); }
    .wrap { max-width:880px; margin:24px auto; padding:0 16px; }
    .card { background:#fff; border-radius:12px; box-shadow:0 2px 10px rgba(0,0,0,.06); padding:20px; }
    header { text-align:center; margin-bottom:20px; }
    header img { height:80px; }
    h1 { margin:6px 0 0; color:var(--red); }
    .muted{color:var(--muted);}
    .btn{border:0;padding:10px 14px;border-radius:10px;font-weight:600;cursor:pointer;}
    .btn-primary{background:var(--red);color:#fff;}
    .btn-secondary{background:#fff;color:var(--red);border:1px solid var(--red);}
    input,select,textarea{width:100%;padding:10px;border:1px solid #ccc;border-radius:8px;font:inherit;}
    table{width:100%;border-collapse:collapse;}
    th,td{padding:8px;text-align:left;}
    tr:nth-child(even){background:#f9f9f9;}
    .success{background:#eef8f0;border:1px solid #cfe8d6;color:var(--ok);padding:10px;border-radius:10px;}
    .alert{background:#fff3f3;border:1px solid #ffd4d4;color:#7a0f0f;padding:10px;border-radius:10px;}
  </style>
</head>
<body>
  <div class="wrap">
    <header>
      <img src="/logo.png" alt="Johanniter Logo">
      <h1>Johanniter Österreich – CIRS</h1>
    </header>
    <main><%- body %></main>
    <footer class="muted" style="text-align:center; margin-top:30px;">© Johanniter Österreich – CIRS</footer>
  </div>
</body>
</html>
