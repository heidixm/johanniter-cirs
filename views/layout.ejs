<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title><%= title || 'Johanniter Österreich – CIRS' %></title>

  <!-- Favicon auf Basis des lokalen Logos -->
  <link rel="icon" type="image/png" href="/logo.png" />

  <!-- Schrift -->
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet" />

  <style>
    :root{
      --bg:#f6f7fb; --card:#fff; --text:#1f2937; --muted:#6b7280;
      --brand:#b0001c; --brand-600:#8e0017; --line:#e5e7eb;
      --focus:#2563eb; --success:#10b981;
    }
    *{box-sizing:border-box}
    html,body{margin:0;height:100%}
    body{
      font-family:"Inter",system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial,sans-serif;
      background:var(--bg); color:var(--text); line-height:1.5;
    }

    /* Sticky Header */
    .header{position:sticky; top:0; z-index:20; background:rgba(255,255,255,0.9);
      backdrop-filter:saturate(150%) blur(8px); border-bottom:1px solid var(--line);}
    .header-inner{max-width:980px; margin:0 auto; padding:10px 16px; display:flex; align-items:center; gap:14px;}
    .logo{display:flex; align-items:center; gap:10px; text-decoration:none; color:inherit;}
    .logo img{height:44px; width:auto; display:block;}
    .app-title{font-weight:700; font-size:18px; letter-spacing:.2px;}

    /* Page container */
    .container{ max-width:980px; margin:24px auto; padding:0 16px; }

    /* Card */
    .card{background:var(--card); border:1px solid var(--line); border-radius:16px;
      box-shadow:0 8px 30px rgba(17,24,39,.06); padding:20px;}
    h1,h2{margin:0 0 10px}
    .muted{color:var(--muted)}

    /* Actions / Buttons */
    .actions{display:flex; gap:10px; flex-wrap:wrap; margin:0 0 14px}
    .btn{appearance:none; border:1px solid var(--brand); background:var(--brand);
      color:#fff; padding:10px 14px; border-radius:10px; text-decoration:none; font-weight:600; cursor:pointer;
      transition:transform .05s ease, background .2s ease;}
    .btn:hover{background:var(--brand-600)}
    .btn.secondary{background:#fff; color:var(--brand); border-color:var(--brand);}
    .btn.secondary:hover{background:#fff0f2}

    /* Table */
    .table{width:100%; border-collapse:separate; border-spacing:0; overflow:hidden; border-radius:12px; border:1px solid var(--line)}
    .table th, .table td{padding:12px 14px; border-bottom:1px solid var(--line)}
    .table thead th{background:#fafafa; text-align:left; font-weight:700}
    .table tbody tr:last-child td{border-bottom:none}
    .table a{color:var(--brand); text-decoration:none; font-weight:600}

    /* Forms */
    form .grid{display:grid; gap:14px}
    @media(min-width:760px){ form .grid{grid-template-columns:1fr 1fr} }
    label{display:flex; flex-direction:column; gap:6px; font-weight:600}
    input[type=text], input[type=datetime-local], input[type=email], textarea{
      padding:10px 12px; border:1px solid var(--line); border-radius:10px; background:#fff; color:inherit; outline:none;
    }
    input:focus, textarea:focus{border-color:var(--focus); box-shadow:0 0 0 4px rgba(37,99,235,.12)}
    textarea{resize:vertical}
    .success{background:rgba(16,185,129,.10); border:1px solid rgba(16,185,129,.35); color:#065f46; padding:10px 12px; border-radius:10px;}

    /* Footer */
    footer{color:var(--muted); text-align:center; padding:28px 0}
  </style>
</head>
<body>
  <header class="header">
    <div class="header-inner">
      <a class="logo" href="/">
        <img src="<%= logoUrl || '/logo.png' %>" alt="Johanniter Logo">
        <div class="app-title">CIRS</div>
      </a>
    </div>
  </header>

  <main class="container">
    <%- body %>
  </main>

  <footer>© Johanniter Österreich – CIRS</footer>
</body>
</html>
